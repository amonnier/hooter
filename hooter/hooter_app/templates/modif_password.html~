{% extends '_base.html' %}
{% block titre %}  Modification du mot de passe{% endblock %}
{%block autre%}

{% endblock %}

{% block contenu %}
<div class="page-header">
  <h1>{{ utilisateur.nom }} <small>@{{ utilisateur.pseudo }}</small></h1>
</div>

<form action="{% url 'modif_password' utilisateur.pseudo %}" method="POST">

	<div class="input-group">
		<span class="input-group-addon">Mot de passe :</span>
		 <input type="password" class="form-control" id="password" placeholder="Mot de passe">
		
	</div>
	<br/>
	
	<div class="input-group">
		<span class="input-group-addon">Confirmation du mot de passe :</span>
		 <input type="password" class="form-control" id="confirmation" placeholder="Mot de passe">
		
	</div>
	<br/>

<button type="submit"  value="modif_password" class="btn btn-primary btn-sm">Enregistrer</button>
{% csrf_token%}
</form>

{% endblock %}
